# Legacy fallback only. Preferred workflow: use `uv sync` with `pyproject.toml` + `uv.lock`.
fastapi>=0.116.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
greenlet>=3.1.0
aiosqlite>=0.20.0
pydantic>=2.8.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
